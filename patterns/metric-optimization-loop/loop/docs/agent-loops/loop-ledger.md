# Metric Optimization Ledger

## Current Handoff

- Next item: EXP-001
- Next step: read `autoresearch.md`, confirm the baseline, propose one experiment, then run benchmark and checks.
- Scope guard: do not change the benchmark or correctness checks unless the queue item explicitly asks for it.
- First validation: `./autoresearch.sh`, `./autoresearch.checks.sh`, inspect `autoresearch.jsonl`.

## Entries

### Loop 0 — bootstrap

- status: initialized
- validation: baseline pending
- notes: metric factory ready
